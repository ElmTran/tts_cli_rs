[package]
name = "tts_cli_rs"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = "4.5.4"
dotenv = "0.15.0"
reqwest = { version = "0.12.3", features = ["default", "blocking", "json"] }
tokio = { version = "1", features = ["full"] }
